<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Website</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css"
        integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
</head>

<body>
    <div class="container">
    <h1>All Blogs</h1>
    <a href="/article/new" class="btn btn-primary my-3">Add Articles</a>
        <% articles.forEach(article => { %>
        <div class="card my-3" style="width: 18rem;">
            <div class="card-body">
                <h5 class="card-title my-0"><%= article.title %></h5>
                <small class="text-muted my-2"><%= article.time.toLocaleDateString() %></small>
                <p class="card-text"><%= article.description %></p>
                <a href="/article/<%=article.slug%>" class="btn btn-primary">Read More</a>
                <a href="/article/edit/<%=article.slug%>" class="btn btn-secondary">Edit</a>
                <form action="/article/<%= article.id %>?_method=DELETE" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>
            </div>
        </div>
        <% }) %>
    </div>
</body>

</html>